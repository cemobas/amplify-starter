/**
 * Shared code for `apps/web` (Next.js) and `apps/mobile` (Expo).
 * Keep imports here platform-agnostic (no `next/*`, no `react-native`).
 */

export const appWorkspaceTag = "amplify-starter-monorepo";

export { randomWarsawAddress } from "./randomWarsawAddress";
