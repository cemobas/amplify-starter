; Nested @aws-amplify/* bundles declare peer zod@3.25.17 while npm nests another
; copy — npm then prints ERESOLVE even when versions match. Relaxed peers are
; normal for Amplify Gen2 + CDK toolchains.
legacy-peer-deps=true
